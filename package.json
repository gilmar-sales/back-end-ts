{
  "name": "back-end",
  "version": "1.0.0",
  "description": "Exemplo para criação de API com Autenticação",
  "main": "index.ts",
  "author": "Gilmar Sales",
  "license": "MIT",
  "scripts": {
    "start": "node ./dist/index.js",
    "dev": "ts-node-dev --respawn --transpile-only -r tsconfig-paths/register .",
    "build": "tsc --project tsconfig.json && tscpaths -p tsconfig.json -s . -o ./dist"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^17.0.38",
    "ts-node-dev": "^2.0.0",
    "tsconfig-paths": "^4.0.0",
    "tscpaths": "^0.0.9",
    "typescript": "^4.7.2"
  },
  "dependencies": {
    "express": "^4.18.1"
  }
}
